{
  "name": "jarvis-hud-frontend-tests",
  "private": true,
  "type": "module",
  "description": "Test harness for the HUD's frontend. The backend is Python/pytest; this covers static/js.",
  "scripts": {
    "test": "node --test \"tests/js/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
